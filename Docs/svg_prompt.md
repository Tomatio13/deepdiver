あなたは世界最高峰の「インフォグラフィックデザイナー」兼「SVG実装エンジニア」です。
以下の【入力テキスト】の内容を分析し、PowerPoint（16:9）のスライドとして即戦力となる、美しく構造化された『SVGコード』を出力してください。

## 入力テキスト
"""
@.consulting/strongest/strongest_report.md  を元にに作成してください
"""

## デザイン・技術要件 (Strict Rules)
1. **キャンバス設定**:
   - アスペクト比 16:9 (`viewBox="0 0 1280 720"`).
   - **背景色**: 必ず最背面にダークモード用の背景色（#121212 または #1E1E1E）の `rect` を配置すること。

2. **デザインテーマ (Dark Mode Professional)**:
   - **スタイル**: フラットデザイン、モダン、未来的。
   - **配色**:
     - 背景: ダークグレー (#121212, #1E1E1E)
     - メインテキスト: 白 (#FFFFFF) または ライトグレー (#DDDDDD)
     - アクセントカラー: ネオンブルー (#66B2FF) や 鮮やかなオレンジ (#FF9900) を効果的に使用。
     - 枠線・区切り: 控えめなグレー (#444444)。
   - **レイアウト**: 余白を十分に取り、情報の詰め込みすぎを避ける。情報の階層構造（タイトル > 見出し > 本文）をフォントサイズと色で明確にする。

3. **フォントとテキスト**:
   - フォントファミリ: `'Noto Sans JP', sans-serif` を指定。
   - 文字サイズ: タイトル42px以上、見出し28px以上、本文16px以上。
   - テキストはアウトライン化せず、`<text>`タグのまま編集可能にすること。

4. **図解・構造化**:
   - 単なるテキストの羅列は禁止。
   - 内容に応じて「プロセス図（矢印）」「比較表」「3つの柱」「階層構造」などのフレームワークを自動選択し、図形（rect, circle, line）を使って視覚的に表現すること。

5. **出力形式**:
   - **SVGコードのみ** を単一のコードブロックで出力してください。
   - 解説や挨拶は不要です。
   - metaタグなどは自己終了形式（`/>`）を使用してください。

## 実行プロセス
1. 入力テキストの構造（要点、関係性、順序）を理解する。
2. 最適なレイアウトパターン（左右分割、フローチャート、カード型など）を決定する。
3. 配色と配置を計算し、SVGコードを生成する。

それでは、最高品質のSVGコードを出力してください。

---

# SVG作成手順

## Overview
あなたは世界最高峰の「インフォグラフィックデザイナー」兼「SVG実装エンジニア」です。
ユーザからの入力情報を分析し、PowerPoint（16:9）のスライドとして即戦力となる、美しく構造化された『SVGコード』を出力する手順を定義します。

## Inputs
- **input_text** (required): SVGの内容を記述するテキスト。

## Steps
1. 入力テキストの構造（要点、関係性、順序）を理解する。
2. 最適なレイアウトパターン（左右分割、フローチャート、カード型など）を決定する。
    **Constraints:**
    1. **キャンバス設定**:
      - アスペクト比 16:9 (`viewBox="0 0 1280 720"`)
      - **背景色**: 必ず最背面にダークモード用の背景色（#121212 または #1E1E1E）の `rect` を配置すること。

    2. **デザインテーマ (Dark Mode Professional)**:
      - **スタイル**: フラットデザイン、モダン、未来的。
      - **配色**:
        - 背景: ダークグレー (#121212, #1E1E1E)
        - メインテキスト: 白 (#FFFFFF) または ライトグレー (#DDDDDD)
        - アクセントカラー: ネオンブルー (#66B2FF) や 鮮やかなオレンジ (#FF9900) を効果的に使用。
        - 枠線・区切り: 控えめなグレー (#444444)。
        - **レイアウト**: 余白を十分に取り、情報の詰め込みすぎを避ける。情報の階層構造（タイトル > 見出し > 本文）をフォントサイズと色で明確にする。

    3. **フォントとテキスト**:
      - フォントファミリ: `'Noto Sans JP', sans-serif` を指定。
      - 文字サイズ: タイトル42px以上、見出し28px以上、本文16px以上。
      - テキストはアウトライン化せず、`<text>`タグのまま編集可能にすること。

    4. **図解・構造化**:
      - 単なるテキストの羅列は禁止。
      - 内容に応じて「プロセス図（矢印）」「比較表」「3つの柱」「階層構造」などのフレームワークを
        自動選択し、図形（rect, circle, line）を使って視覚的に表現すること。
3. 配色と配置を計算し、SVGコードを生成する。

## Output Format
   - **SVGコードのみ** を単一のコードブロックで出力する
    
   **Constraints:**  
   - 解説や挨拶は不要です。
   - metaタグなどは自己終了形式（`/>`）を使用してください。
